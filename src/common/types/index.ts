export enum Status {
  standard= 'standard',
  select = 'select',
  premium = 'premium',
}

export enum Role {
  newbie = 'newbie',
  student = 'student',
  teacher = 'teacher',
}

export enum Sex {
  m = 'm',
  f = 'f',
}
